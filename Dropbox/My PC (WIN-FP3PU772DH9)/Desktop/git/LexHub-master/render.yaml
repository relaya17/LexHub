services:
  - type: web
    name: lexhub
    env: node
    plan: free
    buildCommand: chmod +x build.sh && ./build.sh
    startCommand: pnpm start
    envVars:
      - key: NODE_ENV
        value: production
